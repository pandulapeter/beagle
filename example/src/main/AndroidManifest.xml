<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.pandulapeter.beagleExample">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.pandulapeter.beagleExample.BeagleExampleApplication"
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/SplashTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name="com.pandulapeter.beagleExample.screens.MainActivity">

            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>

        <activity android:name="com.pandulapeter.beagleExample.screens.LoginActivity" />

    </application>

</manifest>